:80 {
    log {
        output stdout
        format json
        level DEBUG
    }

    handle /api/* {
        reverse_proxy {env.BACKEND_URL}
    }

    handle * {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
