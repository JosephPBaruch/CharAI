:80 {
    log {
        output stdout
        format json
        level {$LOG_LEVEL} # DEBUG, INFO, WARN, ERROR
    }

    handle /api/* {
        reverse_proxy {$BACKEND_URL}
    }

    handle * {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
