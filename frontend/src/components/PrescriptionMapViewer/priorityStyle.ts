// MapLibre GL paint expression to color polygons by priority.
// Supports `properties.priority` with fallback to `properties.priorityRange`.
// Fallback color is #90caf9.
export { priorityFillColorExpression } from '../../types/maplibre/priorityStyle';
