{
  "name": "charai-functional-tests",
  "version": "1.0.0",
  "description": "Functional tests for CharAI using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "HEADLESS=false playwright test",
    "test:report": "playwright test --reporter=html"
  },
  "devDependencies": {
    "@playwright/test": "1.58.0",
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  }
}
