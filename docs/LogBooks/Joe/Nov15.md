# November 15, 2025

Containerized the frontend and backend application. I want to make it configurable for the different deployments types and are super easy for a user to start the application (not needing to worry about dependencies etc).

Waiting to open up a PR until we get the frontend and backend stuff into main.
