# Caddy

## Mac

```sh
    brew install caddy

    caddy run
```

## Linux

1. Install and set up caddy

   Caddy runs as a systemd service

2. Place the Caddyfile in the correct place

   Typically `/etc/caddy/Caddyfile`

3. Restart caddy

```sh

    systemctl restart caddy

```
