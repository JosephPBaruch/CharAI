from django.contrib import admin

"""Django admin site configuration for the `core` app.

Register models here so they appear in the admin UI,

To create an administrator account run::

	python manage.py createsuperuser

Then sign in at `/admin/` with the credentials you create.
"""
