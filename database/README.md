# Testing Database

This directory contains a lightweight database setup used to simulate the production database for testing and CI workflows.

Use this test database when you want to validate the application in an isolated environment or when running the project's Docker-based pipelines.

## Quick usage

- Run the database pipeline from the repository root (builds and runs the configured containers):

```sh
./pipeline.sh
```
